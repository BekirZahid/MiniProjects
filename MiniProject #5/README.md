# Guessing the Department Tool
The tool uses classification algorithm like Naive Bayes and Fisher
algorithm to train the data, then guessing the department of selected
professor with using trained data.
